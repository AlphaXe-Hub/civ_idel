export const EUREKA_IDS = Array.from({ length: 32 }, (_, i) => `eur_${String(i + 1).padStart(2, "0")}`);
export const GREAT_PERSON_IDS = Array.from({ length: 32 }, (_, i) => `gp_${String(i + 1).padStart(2, "0")}`);
export const RELIC_IDS = Array.from({ length: 32 }, (_, i) => `rel_${String(i + 1).padStart(2, "0")}`);
